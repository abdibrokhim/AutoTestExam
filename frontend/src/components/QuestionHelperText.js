import React from 'react';

const QuestionHelperText = ({ comment }) => {
    return (
        <div className=''>
            <div>{comment}</div>
        </div>
    );
}

export default QuestionHelperText;