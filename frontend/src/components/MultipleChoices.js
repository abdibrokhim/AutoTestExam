import React from 'react';

const MultipleChoices = ({ options }) => {
    return (
        <div className='col'>
            <div>{options}</div>
        </div>
    );
}

export default MultipleChoices;